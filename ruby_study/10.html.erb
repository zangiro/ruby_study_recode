lines = $stdin.readlines.map{|a|a.chomp}
amount = lines[0].to_i
array = lines[1].split.map{|a|a.to_i}
total = 0
array.each do |arr|
  total += arr
end

x = total / amount
y = total % amount
if y > 0
  x += 1
end
p x